% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_modules.R
\name{mcmodule_composition}
\alias{mcmodule_composition}
\title{Describe Monte Carlo Module Compostion}
\usage{
mcmodule_composition(mcmodule)
}
\arguments{
\item{mcmodule}{An mcmodule object to check}
}
\value{
A list with elements:
\itemize{
\item \code{is_combined}: Logical. TRUE if module is combined, FALSE if raw
\item \code{n_modules}: Integer. Number of component modules (1 for raw, >1 for combined)
\item \code{module_names}: Character vector. Names of all component modules (recursive)
\item \code{module_exp}: Data frame with columns \code{module} and \code{exp} showing all expressions per module
}
}
\description{
Determines whether an mcmodule is a raw (single) module or a composition of
multiple modules combined via \code{combine_modules()}. For combined modules,
returns the names of all component modules (recursively extracted up to one level).
}
\details{
A raw module has a single expression in \code{mcmodule$exp}.
A combined module has multiple expressions in \code{mcmodule$exp}, each representing
a component module that was combined via \code{combine_modules()}.

For combined modules, module names are recursively extracted up to one level deep.
This allows identifying all base modules even in deeply nested combinations.
}
\examples{
# Raw module
raw_module <- eval_module(
  exp = c(imports = imports_exp),
  data = imports_data,
  mctable = imports_mctable,
  data_keys = imports_data_keys
)
mcmodule_composition(raw_module)

# Combined module
combined_module <- combine_modules(raw_module, raw_module)
mcmodule_composition(combined_module)

}
