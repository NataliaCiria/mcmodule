% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/totals.R
\name{agg_variates}
\alias{agg_variates}
\title{Aggregate mcnode Values Across Groups}
\usage{
agg_variates(
  mcmodule,
  mc_name,
  agg_keys = NULL,
  agg_suffix = NULL,
  prefix = NULL,
  name = NULL,
  summary = TRUE,
  keep_variates = FALSE,
  agg_func = NULL
)
}
\arguments{
\item{mcmodule}{An \code{mcmodule} object. Module containing node list and data.}

\item{mc_name}{Character. Name of node to aggregate.}

\item{agg_keys}{Character vector, optional. Column names for grouping.
If \code{NULL}, defaults to \code{"scenario_id"}. Default: \code{NULL}.}

\item{agg_suffix}{Character, optional. Suffix for aggregated node name.
Default: \code{NULL}.}

\item{prefix}{Character, optional. Prefix for output node name. Default: \code{NULL}.}

\item{name}{Character, optional. Custom name for output node. Default: \code{NULL}.}

\item{summary}{Logical. If \code{TRUE}, include summary statistics. Default: \code{TRUE}.}

\item{keep_variates}{Logical. If \code{TRUE}, preserve individual variate values.
Default: \code{FALSE}.}

\item{agg_func}{Character or function, optional. Aggregation method:
\code{"prob"} for combined probability, \code{"sum"} for the sum, or \code{"avg"} for
the mean. A custom function must accept a list of \code{mcnode} objects and
return one aggregated \code{mcnode}. If \code{NULL}, \code{"prob"} is used.
Default: \code{NULL}.}
}
\value{
An \code{mcmodule} object with a new aggregated node added.
}
\description{
Aggregates node values across grouping variables using various methods
(combined probability, sum, mean, or a custom function). Returns an
updated mcmodule with a new aggregated node.
}
\details{
If sample-design nodes are aggregated, the resulting node will be equal
to the original node, but with the \code{"agg_total"} type and summary statistics
added.
}
\examples{
imports_mcmodule <- agg_variates(
  imports_mcmodule, "no_detect",
  agg_keys = c("scenario_id", "pathogen")
)
print(imports_mcmodule$node_list$no_detect_agg$summary)

}
\references{
Ross SM (2014). \emph{Introduction to Probability Models}, 11th ed. Elsevier.
}
\seealso{
\code{\link[=at_least_one]{at_least_one()}} for combining probability nodes,
\code{\link[=trial_totals]{trial_totals()}} for repeated or hierarchical Bernoulli trials,
\code{\link[=mc_filter]{mc_filter()}} for selecting variates, and \code{\link[=mc_summary]{mc_summary()}} for node
summaries.
}
