# Checks mctable data

Checks mctable data

## Usage

``` r
check_mctable(data)
```

## Arguments

- data:

  A data frame containing MC table information. Must contain an 'mcnode'
  column.

## Value

A data frame with the standard mctable structure
